\DeclareAcronym{CCA}{
   short = CCA,
   long = Congestion Control Algorithm,
   short-plural = s,
   long-plural = s,
   sort = Congestioncontrolalgorithm
}

\DeclareAcronym{BBR}{
   short = BBR,
   long = Bottleneck Bandwidth and Round-trip propagation time,
   short-plural = s,
   long-plural = s,
   sort = Bottleneckbandwidthandroundtrippropagation
}

\DeclareAcronym{TCP}{
   short = TCP,
   long = Transmission Control Protocol,
   short-plural = s,
   long-plural = s,
   sort = TransmissionControlProtocol
}

\DeclareAcronym{BDP}{
   short = BDP,
   long = Bandwidth-Delay product,
   short-plural = s,
   long-plural = s,
   sort = Bandwidth-Delayproduct
}

\DeclareAcronym{RTT}{
   short = RTT,
   long = Round-Trip Time,
   short-plural = s,
   long-plural = s,
   sort = Round-TripTime
}